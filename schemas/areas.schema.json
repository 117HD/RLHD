{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "areas.schema.json",
  "title": "Area Data",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "required": [ "description" ],
    "properties": {
      "description": {
        "type": "string",
        "description": "Description of which area the properties apply to."
      },
      "aabbs": {
        "type": "array",
        "description": "An array of axis-aligned bounding boxes defining the area.",
        "items": {
          "$ref": "aabb.schema.json"
        }
      },
      "hideOtherRegions": {
        "type": "boolean",
        "description": "If you want regions around the aabbs to be hidden."
      },
      "horizonTile": {
        "description": "Data for a fake tile into the horizon.",
        "items": {
          "$ref": "horizon.schema.json"
        }
      }
    }
  }
}
