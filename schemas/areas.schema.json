{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "areas.schema.json",
  "title": "List of area definitions",
  "type": "array",
  "items": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "description": {
        "type": "string",
        "description": "Description of the area that the properties apply to."
      },
      "aabbs": {
        "type": "array",
        "description": "An array of axis-aligned bounding boxes outlining the area.",
        "items": {
          "$ref": "aabb.schema.json"
        }
      },
      "region": {
        "type": "int",
        "description": "A region ID as an alternative to AABBs."
      },
      "hideOtherRegions": {
        "type": "boolean",
        "description": "Whether regions outside of the area should be hidden."
      },
      "horizonTile": {
        "description": "Definition of a horizon tile that fills in the scene edges.",
        "items": {
          "$ref": "horizon_tile.schema.json"
        }
      }
    }
  }
}
