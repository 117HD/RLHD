{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "aabb.schema.json",
  "title": "Axis-Aligned Bounding Box",
  "type": [ "integer", "string", "array" ],
  "description": "Specify a region ID, area enum name, or the X and Y world coordinates for a single point, or two points on the corners of a bounding box, optionally including a plane per point. One of: RegionID, XY, XYZ, XYXY, XYXYZ, XYZXYZ.",
  "minItems": 2,
  "maxItems": 6,
  "items": {
    "type": "number"
  }
}
