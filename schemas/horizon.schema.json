{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "horizon.schema.json",
  "title": "Horizon Tile",
  "type": "object",
  "description": "Specify horizon tile data.",
  "required": [ "material" ],
  "properties": {
    "material": {
      "type": "enum",
      "description": "The Main material you wish to render."
    },
    "materialBelow": {
      "type": "enum",
      "description": "The Bottom material you wish to render under things like water."
    },
    "isOverlay": {
      "type": "boolean",
      "description": "If the tile is an overlay."
    },
    "waterType": {
      "type": "enum",
      "description": "The type of water you want to render."
    },
    "height": {
      "type": "integer",
      "description": "Height of the tile."
    },
    "color": {
      "type": "integer",
      "description": "Color of the tile."
    }
  }
}
